from setuptools import setup

setup(
   name='PythonUI',
   version='0.1',
   description='A simple wrapper for creating a graphical user interface in ' +
               'python using PyQt5',
   author='Thomas Oberbichler',
   packages=['python_ui'],
   install_requires=[],
)
